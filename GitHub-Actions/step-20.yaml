      - name: Show which event triggered this run
        run: |
          echo "This workflow was triggered by: ${{ github.event_name }}"
          echo "On the repository: ${{ github.repository }}"
          echo "By user: ${{ github.actor }}"
          echo "Ref (branch or tag): ${{ github.ref }}"
