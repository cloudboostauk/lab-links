cat << 'EOF' > catalog-nodeport.yaml
apiVersion: v1
kind: Service
metadata:
  name: catalog-external
spec:
  type: NodePort
  selector:
    app: techstart
    component: catalog
  ports:
  - port: 80
    targetPort: 5000
    nodePort: 30080
EOF
